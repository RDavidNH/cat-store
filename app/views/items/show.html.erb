<br>
<div class="container">
    <div class="row">
        <div class="col-sm-6">
            <%= image_tag 'sleepcutecat.jpg', width: '100%' %>
        </div>
        <div class="col-sm-6">
            <table class="table table-hover">
                <tr>
                    <th>Titre</th>
                    <td><%= @current_item.title %></td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>$<%= @current_item.price %></td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td><%= @current_item.description %></td>
                </tr>
            </table>
        </div>
    </div>
</div>
